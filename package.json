{
  "name": "gestion-des-ressources-cloud",
  "version": "1.0.0",
  "description": "Un outil simple pour gérer et visualiser les ressources cloud d'AWS à partir d'une interface web.",
  "main": "src/server/index.js",
  "scripts": {
    "start": "node src/server/index.js",  // Démarre le serveur
    "client": "npm start --prefix client",  // Démarre le client React
    "dev": "concurrently \"npm run start\" --prefix server \"npm run client\""  // Démarre les serveurs du client et du serveur en parallèle
  },
  "dependencies": {
    "express": "^4.17.1",
    "react": "^17.0.2",
    "react-dom": "^17.0.2",
    "concurrently": "^6.0.0"
  }
}